# 🛫 Passenger Satisfaction Tracker

ML-система для прогнозирования удовлетворённости пассажиров авиакомпании в реальном времени. 

---

## 💼 Бизнес-ценность

Авиакомпании теряют миллионы долларов из-за низкой лояльности пассажиров, негативных отзывов и отказов от повторных полётов.

Наш сервис решает эту проблему, позволяя бизнесу:

- Целенаправленно улучшать сервис (например, усилить работу персонала на рейсах с высоким риском недовольства),
- Отслеживать просадки удовлетворенности пассажиров (например, уделить внимание работе с детьми на рейсах, если аналитические панели показывают низкую степень удовлетворенности детей)
- Оптимизировать затраты — вместо улучшения всех направлений, фокусироваться на ключевых драйверах неудовлетворённости (например, Wi-Fi, багаж, задержки),
- Повышать лояльность клиентов, что напрямую влияет на повторные продажи и репутацию.

> 💡 Пример: если модель предсказывает низкую удовлетворённость у пассажира в Eco-классе из-за задержки, авиакомпания может автоматически отправить ему компенсационный промокод на следующий перелет — и превратить негатив в лояльность.

---

## 🏗 Архитектура

![Архитектурная схема](schema.png)

Система состоит из следующих компонентов:

- Streamlit UI — загрузка данных, отправка в Kafka, визуализация результатов с помощью аналитических панелей, фильтрации
- Kafka — брокер сообщений для асинхронной обработки
- ML Scoring Service (`satisfaction_tracker`) — сервис инференса, использующий под капотом CatBoost
- PostgreSQL — хранение предсказаний и метаданных
- DB Writer — потребитель Kafka, сохраняющий результаты в БД

Все компоненты упакованы в Docker и запускаются через `docker-compose`

---

## 🚀 Запуск проекта

### Требования
- Docker
- Docker Compose

### Инструкция
1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/princebelkovetz/final_project
   cd final_project
   ```

2. Соберите и запустите:
   ```bash
   docker compose up --build
   ```

3. Загрузите файл `train.csv` из [соревнования](https://www.kaggle.com/datasets/teejmahal20/airline-passenger-satisfaction) и положите в папку /satisfaction_tracker/data/ через UI

4. Откройте интерфейсы:
   - Streamlit UI: http://localhost:8501
   - Kafka UI: http://localhost:8080


5. Загрузите файл формата `test.csv` из соревнования через UI, отправьте в систему и посмотрите аналитику.

---

## 📊 Возможности интерфейса

- 📤 Отправка CSV с данными о полётах в Kafka
- 📈 Аналитическая панель с графиками:
  - Доля удовлетворённых по полу, классу, типу клиента
- 🔍 Фильтрация данных (по классу, типу клиента, возрасту)

---
## 📁 Структура проекта

```
.
├── docker-compose.yaml     # Оркестрация всех сервисов
├── satisfaction_tracker/   # Kafka-консюмер + ML-модель
├── interface/              # Streamlit UI
├── scoring_db_writer/      # Запись предсказаний в PostgreSQL
└── docs/                   # Документация (архитектурная схема)
```

---

## 📝 Автор
Бельковец Григорий & Хасанов Александр (группа БПМИ221)
---

> 💡 Примечание: Проект соответствует всем требованиям финального задания, включая 2 критерия повышенной сложности:  

> ✅ Продвинутый UI с аналитикой

> ✅ Инференс-сервис в связке с брокером сообщений

